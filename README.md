# DocOps MCP Assistant

**AI-ассистент для работы с документацией через MCP-серверы (Git + Confluence + VectorStore)**
*Gradio-приложение + 3 MCP-сервера + демонстрационные репозитории.*

## О проекте

`DocOps MCP Assistant` — это  AI-агент, построенный на технологии **MCP (Model Context Protocol)**.
Агент взаимодействует с **внешними MCP-серверами**, ответственными за различные функции (см. таблицу ниже).

| MCP-Server                | Функциональность                                                    |
| ------------------------- | ------------------------------------------------------------------- |
| **Git MCP Server**        | Чтение Markdown-файлов, перечисление каталогов, поиск по коду/докам |
| **Confluence MCP Server** | Поиск страниц, создание страниц (макет), получение контента         |
| **Vector MCP Server**     | Поиск в готовом векторном индексе документов (JSONL-база)           |

Клиентская часть реализована на **Gradio**, агент — на Python.

## Ключевые возможности

* **AI-поиск по документации** (Git + VectorStore)
* **Работа с Confluence через MCP** (поиск, черновое создание страниц)
* **Git-навигация через MCP** — list_files, read_file
* **LLM-ответы с контекстом** из реальной документации
* **Полный набор тестов** для MCP-серверов и UI/агента
* **Автогенерация демо-репозиториев** (`seed_demo_data.py`)
* **Docker-Compose для запуска всей системы**
* **Makefile** для dev/test/workflow задач

## 🧩 Архитектура

```text
┌───────────────────────┐
│     Gradio UI         │
│  (app/main.py)        │
└───────────┬───────────┘
            │ вызывает
┌───────────▼───────────┐
│   DocOpsAgent         │
│ (app/agent/*)         │
└───────────┬───────────┘
            │ через MCP Client
┌───────────▼─────────────┐
│    MCP Client Layer     │
│  (git / conf / vector)  │
└───────────┬─────────────┘
   вызывает MCP-серверы
            │
─────────────────────────────────────
│ Git MCP Server        (localhost:8760)
│ Confluence MCP Server (localhost:8761)
│ Vector MCP Server     (localhost:8762)
─────────────────────────────────────
```

## Требования

* Python **3.10+**
* pip
* OpenAI API Key
* Windows/macOS/Linux


## Установка и запуск

### Через bat-файл:
Дважды кликните по файлу `run_all.bat`.

### Через sh-скрипт:

1. Сделайте скрипт `run.sh` исполняемым:

```bash
chmod +x run_all.sh
```

2. Запустите скрипт:

```bash
./run_all.sh
```

### С помощью Docker Compose

Выполните команду:

```bash
docker compose up --build
```

## Через Makefile (Linux/macOS)

Если есть GNU make, выполните по очереди:

```bash
make dev      # запуск UI + seed
make mcp      # запустить MCP-серверы
make test     # запустить pytest
```

## Тесты

Выполните команду в корне проекта:

```bash
pytest -q
```

## Структура проекта

```
app/                                # Основное приложение (UI + агент + клиент MCP)
  agent/                            # Логика ИИ-агента
    agent.py                        # Главный класс DocOpsAgent (интеграция MCP + LLM)
    prompts.py                      # Системные промпты и шаблоны
    workflows.py                    # Основные рабочие процессы: QA по документам и др.

  ui/                               # Gradio-интерфейс
    callbacks.py                    # Функции-обработчики событий UI
    components.py                   # Компоненты интерфейса (кнопки, поля, layout-элементы)
    layout.py                       # Высокоуровневый layout Gradio-приложения

  config/                           # Конфигурация (проектная и глобальная)
    projects.yaml                   # Список доступных проектов (demo_repos)
    settings.py                     # Pydantic-настройки + .env параметры

  core/                             # Базовые внутренние механизмы
    llm.py                          # Обертка над OpenAI API (LLM-клиент)
    logging.py                      # Инициализация логирования
    models.py                       # Pydantic-модели, общие типы данных
    storage.py                      # Локальное хранилище (если нужно расширение)

  mcp_client/                       # Клиентская часть, взаимодействие с MCP-серверами
    client.py                       # Управление подключениями к MCP
    confluence_tools.py             # Высокоуровневые операции MCP Confluence
    git_tools.py                    # Высокоуровневые операции MCP Git
    vector_tools.py                 # Высокоуровневые операции MCP Vector Store

mcp-servers/                        # Серверная часть MCP (3 отдельных сервиса)
  git-mcp-server/
    mcp_git/
      github_client.py              # Чтение/поиск файлов в demo репозиториях
      server.py                     # Git MCP Server — реализация MCP протокола
      utils.py                      # Вспомогательные функции
    tests/
      test_git_server.py            # Тесты Git MCP сервера

  confluence-mcp-server/
    mcp_confluence/
      api_client.py                 # HTTP-клиент для mock Confluence API
      server.py                     # Confluence MCP Server
      utils.py                      # Утилиты
    tests/
      test_confluence_server.py     # Тесты Confluence MCP сервера

  vector-mcp-server/
    mcp_vector/
      server.py                     # Vector MCP Server
      store.py                      # Хранилище
    tests/
      test_vector_server.py         # Тесты Vector MCP сервера

scripts/                            # Скрипты для разработки
  dev_tools.py                      # Служебные инструменты/утилиты
  run_app.py                        # Альтернативный запуск UI
  seed_demo_data.py                 # Генерация demo_data/ (Markdown-файлы)
  seed_projects.py                  # Генерация projects.yaml для демо

tests/                              # Интеграционные тесты
  test_agent_flows.py               # Тесты логики агента
  test_mcp_integration.py           # Тесты интеграции MCP клиентов
  test_ui_smoke.py                  # Смоук Gradio UI

# demo_data — динамически создается seed-скриптами
demo_data/
  demo_repos/
    docops-saas/                    # Демо-проект: документация SaaS-платформы
    airport-food/                   # Демо-проект: документация сервиса доставки
```

## Health-check эндпоинт

Для Gradio-сервера предусмотрен FastAPI-эндпойнт:

```
GET /health
```

Возвращает:

```json
{"status": "ok"}
```

## ✨ Пример запроса в UI

В интерфейсе:

```
"Как работает биллинг в DocOps-SaaS?"
```

Ассистент:

* ищет релевантные файлы в Git
* делает поиск в VectorStore
* передает отрывки в LLM
* формирует развернутый ответ

В нижней части UI отображаются "источники".

## Что можно добавить в будущем

* Генерацию документации из пулл-реквестов
* Автоматические рекомендации по улучшению документации
* Автоматическую индексацию Git-репозитория

## Лицензия

MIT

## Если есть проблемы с запуском, тестом и пр., пишите в issues. Конструктивный фидбек и пулл-реквесты приветствуются!